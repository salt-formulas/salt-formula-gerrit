applications:
- gerrit
classes:
- service.gerrit.support
parameters:
  gerrit:
    server:
      enabled: true
      bind:
        address: 0.0.0.0
      mail:
        host: localhost
        from: gerrit
        include_diff: true